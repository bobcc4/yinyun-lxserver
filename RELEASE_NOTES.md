## v1.7.0 (2026-09-13)

### 音云服务端之间分享歌单

- Web 播放器可以为歌单生成带有效期的跨服务端分享链接，并同时导出同内容的 JSON 文件。
- 接收方可以粘贴分享链接或导入 JSON，在导入前查看歌曲总数、本地命中、在线可用和未匹配数量。
- 确认后在接收方当前账户创建普通歌单；全部歌曲都会保留，本地已有文件优先用于播放，不复制或移动音频文件。
- 分享包只包含歌曲名称、歌手、平台和稳定识别信息、专辑及播放时长，不包含密码、Token、代理、本地路径、缓存或临时音频地址。
- 分享链接默认有效 30 天，支持撤销；服务端之间只交换 JSON 元数据，不交换音频文件。
- 匹配仅使用同平台稳定 ID，或同平台歌名、歌手、专辑和时长的精确匹配，不自动使用模糊匹配替换歌曲。
- 本功能只支持音云服务端之间分享，不提供原版洛雪客户端的 `lxmusic://` 或 `.lxmc` 兼容导入。

### 升级说明

- 本版本沿用现有数据目录和 `/api/v1` 接口，可直接升级。
