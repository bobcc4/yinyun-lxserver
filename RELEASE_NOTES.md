## v1.7.2 (2026-09-20)

### Web 播放器初始化与音效兼容性修复

- 本次重新发布补齐下载队列、网络歌单状态和自定义音源列表的登录恢复等待，避免启动阶段携带失效 Token 请求而产生 401。
- 本地歌曲歌词统一读取旁置 LRC 与内嵌标签；没有有效平台编号时返回暂无歌词，不再把 unknown_/local_ 编号交给在线平台而产生 500。歌词查询中的特殊字符正确编码，缓存未命中作为正常结果返回。
- 修复页面启动时设置映射尚未初始化导致的报错，确保同步账户登录状态恢复与 Token 验证能够完整执行。
- 缓存统计等待登录恢复完成；未登录时显示登录提示，不再发送无效统计请求。Token 失效时续签并重试一次，同时合并并发刷新，防止旧账户响应覆盖当前账户界面。
- 保留缓存统计真实故障的 HTTP 状态诊断，接口或网络异常时显示可重试提示。
- 局域网 HTTP 或不支持 AudioWorklet 的浏览器自动禁用变调控件，保留普通播放通路；支持的环境合并变调模块加载，避免重复初始化。
- README 版本徽章改为自动读取 GitHub 最新正式 Release，避免版本说明长期滞后。

### 升级说明

- 沿用现有数据目录和接口，可从 v1.7.1 直接升级；变调需要浏览器支持 AudioWorklet，并通过 HTTPS 或本机地址访问。
