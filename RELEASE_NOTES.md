## v1.7.3 (2026-09-22)

### 歌单侧栏与更新状态修复

- 歌单侧栏操作统一收进“更多”菜单，包括本站分享、跨服务端分享或导出 JSON、改名、更新、打开原始歌单和删除。桌面端悬停或键盘聚焦显示入口，触屏设备常驻显示，名称不再被多个按钮挤占。
- 歌单名称溢出时省略显示，悬停可查看完整名称；菜单支持键盘、点击外部关闭和移动端定位。
- 正常的歌单更新提示改为小圆点，红色感叹号只表示检测失败。
- 修复保存歌单后仅清除浏览器标记的问题，更新状态随服务端保存结果持久化；刷新页面或重启后不会恢复旧标记，更新前发起的后台检查不能覆盖更新后的状态。
- 统一不同平台歌曲编号与歌单元数据的比较，修复同一首歌曲因编号格式不同被误判为歌单有更新。
- 歌单保存失败时保留原内容和更新提示，不再显示更新成功；保留用户自定义歌单名称。

### 更新检查稳定性

- GitHub 更新检查结果缓存 6 小时，并合并并发请求，避免刷新页面反复消耗匿名 API 额度。
- 根据 GitHub 限流响应中的恢复时间暂停重试，等待状态在刷新页面后保留；区分请求额度耗尽与普通 403 拒绝访问。
- 检查失败时保留已知的新版本标记，手动检查显示明确原因和发布页入口，不再误导为 NAS 登录故障或已是最新版。
- 增加请求超时与网络故障重试间隔；在线更新检查被禁用时保持不发送请求。

### 升级说明

- 可从 v1.7.2 直接升级，保留现有数据目录。旧版不可靠的歌单更新标记会失效，后续按新的比较规则重新检测。
- Docker 使用 `bobcc4/yinyun-lxserver:latest` 或固定版本 `bobcc4/yinyun-lxserver:v1.7.3`；更新时拉取新镜像并重建容器。
