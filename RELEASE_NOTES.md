## v1.7.4 (2026-09-22)

### 洛雪歌单文件导入

- “导入 JSON”升级为“导入歌单文件”，保留音云 JSON，并支持洛雪 `.lxmc` 压缩备份和歌单 JSON。
- 支持新旧单歌单、全部歌单以及完整备份中的歌单；完整备份只提取歌单，不恢复设置、账户或音源。
- 导入前显示歌单与歌曲预览，多歌单可勾选导入；同名歌单自动追加序号，不覆盖已有歌单或收藏。
- 保留支持平台的歌曲编号、顺序、专辑与音质信息；原电脑本地文件和缺失有效歌曲信息的条目明确提示并跳过。
- 增加登录校验、上传和解压大小限制、歌曲数量限制及损坏文件提示；不恢复官方洛雪客户端实时同步接口。

### 本地音乐

- 移除顶部子目录筛选入口，本地音乐默认显示全部目录中的歌曲；自动清理旧的子目录筛选条件，保留搜索、音质等其他筛选。
- 移除“根目录／数据目录”切换、设置页缓存位置、文件夹分类及新建文件夹入口，以及原来的跨基础目录移动功能和对应接口。
- 新增缓存和下载统一使用运行目录下的 `cache/<用户名>`、`music/<用户名>`；保留旧数据目录歌曲的自动读取，以及缓存／下载筛选，不自动移动或删除现有文件。
- 删除本地歌曲时按歌曲实际存储位置处理，避免历史数据目录和运行目录中同名歌曲被误删。

### 歌单侧栏显示

- 收藏歌手、收藏专辑与下方歌单统一使用相同的列布局，修复图标、名称及数量错位。
- 自建及收藏歌单的长名称自动循环滚动，短名称保持静止；侧栏尺寸变化或重新展开时重新检测，悬停仍可查看完整名称。

### 升级说明

- 保留原有数据、缓存和音乐目录挂载；无需迁移现有歌曲。外部音乐库仍为独立的只读入口。
- 歌单文件最多 8 MB，解压后最多 16 MB；单文件最多 200 个歌单、30000 首歌曲，单歌单最多 10000 首，转换后的单歌单分享包不超过 2 MB。
- Docker 使用 `bobcc4/yinyun-lxserver:latest` 或固定版本 `bobcc4/yinyun-lxserver:v1.7.4`。
